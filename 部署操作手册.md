# 部署操作手册（照着敲就行）

> 目标机器：你那台 Ubuntu 小主机（用你的 SSH 用户登录，下文以 `用户名@主机地址` 代指）。
> 本手册所有写操作只发生在 `~/yunmei` 里，**不会碰 AstrBot / NapCat 的任何文件**。
> 唯一的系统级改动是装一个 systemd 单元，而且脚本会先告诉你它要干什么。
> 完整回滚：`sudo systemctl disable --now yunmei && sudo rm -f /etc/systemd/system/yunmei.service && sudo systemctl daemon-reload && rm -rf ~/yunmei`

---

## 第 0 步 · 把文件拷到小主机

只需要 5 个文件（`开门自动化方案.md`、`桌面一键开门方案.md` 和 `test_*.py` / `run_tests.py` 是可选项，不拷也能用）：

```
yunmei.py  yunmei_server.py  CONFIG.example.json  yunmei.service  setup.sh
```

在**你自己电脑**上执行（Windows PowerShell）：

```powershell
cd 项目所在目录
scp yunmei.py yunmei_server.py CONFIG.example.json yunmei.service setup.sh 用户名@主机地址:~/
ssh 用户名@主机地址
```

> `主机地址` 用小主机在寝室内网的 IP 即可（小主机上 `ip -4 addr` 查看）。

---

## 第 1 步 · 一条命令装好

登进小主机后：

```bash
bash ~/setup.sh
```

脚本会依次做 6 步，**任何一步失败都会停下并告诉你下一步敲什么**。
正常输出长这样（关键字对得上就没问题）：

| 步骤 | 你会看到 | 不对时 |
|---|---|---|
| 0 环境检查 | `Python 3.x` | `sudo apt install python3-venv python3-pip` |
| 1 建目录 | `已放置 yunmei.py …` | 检查磁盘/家目录权限 |
| 2 依赖 | `bleak 0.x.x` | 没公网→见 §附录 A |
| 3 拉配置 | 跳过或生成成功 | 见第 2 步 |
| 4 蓝牙扫描 | `✔ 命中配置里的门锁服务` + `rssi=-6x` | **最重要**，见 §第 3 步 |
| 5 就绪自检 | `结论：✔ 就绪` | 按它给的红字逐项处理 |

现在还没有配置文件，所以第 3、4 步会提示"配置不存在"，**这是预期的**，继续往下。

---

## 第 2 步 · 拉取锁的参数（要联网，只需要这一次）

```bash
cd ~/yunmei
./venv/bin/python yunmei.py 你的学号 你的密码 --dump-config yunmei_config.json
```

成功输出会列出每把锁（`已写入 yunmei_config.json（权限 600）`、`访问 token：…`、`  305  service=00006e40-…`）。

- 文件会自动 `chmod 600`（里面是永久开门钥匙，别 `chmod 644`、别提交仓库、别发群里）。
- 只存了口令的 MD5，**没存明文密码**。
- 脚本**永远不会覆盖已存在的配置文件**；想重来自己动手：`rm yunmei_config.json` 再跑一次。
- 报"登录失败"：账号密码不对，或学校改了接口。报"没有锁"：这个账号没绑定门锁。

---

## 第 3 步 · 验证蓝牙能不能喊到锁（**成败关键，别跳**）

```bash
cd ~/yunmei
./venv/bin/python yunmei_server.py --scan
```

三种结果，对应三种后续：

1. **`✔ 命中配置里的门锁服务 … rssi=-5x`** → 覆盖 OK，直接进第 4 步。
2. **有设备但"没扫到配置里那把锁"** → 大概率是锁在休眠（这类锁不连续广播）。
   让人**在门口用官方 App 点一次开门**，你同时在机器上再跑一次 `--scan`；
   抓到就是覆盖没问题。仍抓不到 → 把 USB 蓝牙适配器用延长线挪到靠近门、无金属遮挡的位置。
3. **"一个 BLE 设备都没扫到"** → 是蓝牙链路本身不通，跟锁无关。按它给的三条排查：
   ```bash
   hciconfig -a              # 要能看到 hci0 且 UP
   rfkill list               # Soft/硬 blocked 都得是 no
   sudo systemctl status bluetooth    # bluetoothd 得在跑
   ```

> 顺带确认一下这块 combo 网卡是二合一：`rfkill list` 里同时出现 Bluetooth 条目即可。
> 服务已内置处理——**开门瞬间会主动退出后台扫描、把射频交还给连接**，避免 combo 网卡
> "边扫边连"必挂的老毛病。所以平时 `--scan` 扫不到时如果服务在跑，可以先停服务再扫。

---

## 第 4 步 · 跑起来，拿到手机的地址

```bash
cd ~/yunmei
./venv/bin/python yunmei_server.py --print-urls
```

输出里有形如 `http://192.168.1.23:8791/?t=abc123…` 的地址。
先在本机验证一次（**会真的开门**，确认你人在门边再敲）：

```bash
curl -X POST 'http://127.0.0.1:8791/api/open?t=上面那个token'
```

返回 `{"ok": true, "msg": "开门成功 电量≈92%"}` = 通了。
返回 `429` = 刚开过，等 5 秒；返回 `502` = BLE 层失败，消息里会带上云端动态密码兜底。

放行端口（只放寝室网段，**千万别做公网端口映射**）：

```bash
sudo ufw allow from 192.168.0.0/16 to any port 8791 proto tcp   # 换成你的实际网段
```

---

## 第 5 步 · 手机上做成桌面按钮

1. 手机连**寝室同一个 Wi-Fi**；
2. 用 Chrome / Edge / Firefox 打开第 4 步那个完整地址（带 `?t=`）；
3. 浏览器菜单 →「**添加到桌面 / 添加到主屏幕**」；
4. 桌面上出现一个钥匙图标的"开门"按钮，**点它 = 开门**（默认打开页面就自动发起）。

因为 token 已经被写进 PWA 的 `start_url`，装完之后图标不需要再带参数。
室友各自点一遍就有各自入口；给别人时把 URL 里的 `t=` 换成 `yunmei_config.json` 里 `tokens` 数组的另一项，一人一个，方便日志溯源。

**不想要"打开即开门"**：改配置 `"allow_auto": false` 重启服务（变成必须点页面上的按钮），
或临时用一次 `?t=xxx&noauto`。

---

## 第 6 步 · 装成开机自启（确认没问题之后再做）

```bash
bash ~/setup.sh --service
journalctl -u yunmei -f
```

它只会新增 `/etc/systemd/system/yunmei.service`，并把路径/用户自动改成你的实际值。
日常管理：

```bash
sudo systemctl restart yunmei      # 改完配置后重启
systemctl status yunmei            # 看状态
journalctl -u yunmei -n 100        # 看最近日志
```

日志默认**不打印 token**，可以放心贴给别人看。

---

## 出问题怎么办

| 现象 | 怎么办 |
|---|---|
| 什么都不确定 | `cd ~/yunmei && ./venv/bin/python yunmei_server.py --selftest` （不开门，逐项体检） |
| 点了没反应 / 手机转圈 | 手机没连寝室内网，或 IP 变了。小主机上 `ip -4 addr` 重新确认，`curl` 本机通不通 |
| 返回 403 | token 不对（多半是复制漏了字符） |
| 返回 429 | 5 秒冷却，正常保护 |
| 返回 502 | BLE 失败。看消息里的动态密码应急；再用 `--scan` 复检信号/休眠问题 |
| 第一次慢、之后快 | 正常。首次扫描学地址，之后走直连（1~2 秒） |
| 一直很慢（~10 秒） | 锁在轮换蓝牙地址。把 `learn_mac` 确认为 `true`，并保持 `bg_scan: true` |
| 想压到亚秒 | 配置里 `"keep_connected": true`（个别适配器不稳，不稳就关掉） |
| 蓝牙彻底没救 | 两条退路：锁键盘输入动态密码（`POST /api/dynpwd`），或门口放 ESP32-C3 中继 |
| 想确认没误改现有项目 | `journalctl -u yunmei` 只涉及 `~/yunmei`；AstrBot/NapCat 全程没被写入 |

---

## 附录 A · 小主机没公网时装 bleak

在小主机上导出依赖清单，从能上网的机器下载 wheel 后拷过去：

```bash
# 能上网的机器（x86_64 Linux 才行，注意和小主机 CPU 架构一致）
pip download bleak -d ./pkgs
scp -r ./pkgs 用户名@主机地址:~/yunmei/
# 小主机
cd ~/yunmei && ./venv/bin/pip install --no-index --find-links=pkgs bleak
```

`bleak` 依赖 `async-timeout` 等纯 Python 包，通常不需要编译。
> 只装不上 bleak 也还有用：`yunmei.py` 是**纯标准库**，`--dynpwd` 取云端动态密码完全不依赖 bleak。

## 附录 B · 想加的功能

| 想法 | 做法 |
|---|---|
| 室友在外面远程开门 | 小主机装 Tailscale，手机也装，URL 换成 `http://<小主机的Tailscale地址>:8791/?t=…`。这是**可选增强**，不是本方案前提 |
| QQ 里发"开门"就开 | AstrBot 加一个自定义插件，内容就是请求 `POST http://127.0.0.1:8791/api/open?t=…`，并加 QQ 号白名单。**改的是你自己新建的插件文件，不动 AstrBot 现有配置** |
| 手机碰门框一下开门 | NFC 标签（3 元）贴门框，写入同一个 URL |
| 完全无页面闪现 | MacroDroid / Tasker / iOS 快捷指令：桌面图标→后台 POST→Toast 显示结果 |
| 蓝牙中继 | 门口 ESP32-C3（约 15 元）执行 BLE，服务器只发 HTTP；需要的话我写固件 |

## 附录 C · 安全底线（这锁的钥匙是静态的）

1. `yunmei_config.json` = 永久钥匙。`chmod 600`，不进仓库、不发群。
2. 绝不做公网端口映射；只在寝室内网可达。
3. 一人一个 token，谁毕业删 `tokens` 里那一行。
4. 保留官方 App 和实体卡/钥匙，别把兜底拆了。
5. 这是学校资产；自用便利和对外传播是两回事。
6. 云端若哪天给报文加了签名或滚动码，BLE 与动态密码两条路都会失效——那时只剩官方 App 和物理方案。
