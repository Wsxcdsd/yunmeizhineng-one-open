# 宿舍云莓锁 · 手机桌面一键开门（最终方案）

> 场景：寝室有一台带**蓝牙+WiFi 网卡**的 Ubuntu 小主机。
> 目标：**手机点一下桌面图标就开门**，不需要 Tailscale、不需要内网穿透、不需要公网域名、不需要装 App。

## 0. 一句话方案

小主机当"蓝牙执行端"，手机只发一个**局域网 HTTP 请求**。
页面只有一个大按钮，且默认**打开页面就自动开门**——所以"点图标"这件事本身就是"开门"这个动作。

```
手机点桌面图标 → http://<小主机局域网IP>:8791/?t=<token> → 后台 POST /api/open
              → 小主机蓝牙按协议写特征 → 锁开 → 手机震动 + 显示"开门成功 电量≈92%"
```

手机侧：**不需要蓝牙、不需要装 App、不需要在门外保持任何进程**。
只要手机和小主机在同一局域网（连寝室 Wi-Fi 即可，路由器断公网也能开）。

---

## 1. 为什么必须这样做（读码结论）

参考 [zxy19/yunmei_unintelligent](https://github.com/zxy19/yunmei_unintelligent)（Android）
与 [zxy19/yunmei_unintelligent_pwa](https://github.com/zxy19/yunmei_unintelligent_pwa)（Web），逐行读完
`UnlockService.java` / `YunmeiAPI.java` / `CodeService.java` / `unlock.js` / `ble.js` 后：

1. **开门是纯蓝牙本地行为，云端不参与。** 云 API 只发钥匙：登录 → 取学校节点 →
   `/dormuser/getuserlock` 拿到 `lockSecret / lockServiceUuid / lockCharacterUuid`，
   之后开门不再需要网络。**不存在"抓一次云端接口就能远程开门"这条路。**
2. **开锁报文没有任何防重放**：
   ```
   d0 | len | lockSecret(ASCII) | a5 | 6位十进制随机数(低位在前) | "ID01" | a7
   ```
   那 6 位是客户端 `Math.random()` 现生成的，**锁不校验它**；无 challenge-response、
   无滚动码、无时间戳、无签名。→ `lockSecret` 是**静态长期密钥**，
   所以"让服务器代你开门"技术上完全成立，手机可以完全不出场。
3. **云 API 无签名、无证书绑定**：form-urlencoded + `token_data`/`token_userId`/`tokenUserId`
   三个头即可。
4. **另有一条纯云端兜底**（`CodeService.java`）：
   `POST {school}/lockpassword/getlockpwdbylockno {lockNo}` → `{"lockPwd":"..."}`
   返回的是**在锁键盘上手输的数字密码**，完全不碰蓝牙。

### 为什么不用那个 PWA 的 Web Bluetooth
- 浏览器每次连接可能弹"选择设备"；免弹窗的 `navigator.bluetooth.getDevices()` 在 MDN 上仍是
  *Limited availability / Experimental*。
- iOS 只能靠 Bluefy 这类第三方浏览器（原作者 README 自己写的）。
- 它要求**手机自己开蓝牙**，而锁在门外，蓝牙穿一道防火门经常连不上。

→ **蓝牙交给服务器做，手机只发 HTTP。** 这是"最方便"和"能落地"的交集。

---

## 2. 部署：5 步，全程只新增文件

> 以下都在**小主机上**，不改动现有 AstrBot / NapCat 的配置与文件。

```bash
# ① 建目录、放文件（只拷这两个 py）
mkdir -p ~/yunmei && cd ~/yunmei      # 把 yunmei.py 和 yunmei_server.py 放进来

# ② 环境与依赖
python3 -m venv venv && ./venv/bin/pip install bleak

# ③ ★ 先做这步：确认网卡蓝牙能不能喊到锁（决定后面要不要挪适配器）
./venv/bin/python yunmei_server.py --scan
#   ✔ 命中 → 打印地址和 rssi，继续往下
#   ✘ 0 个设备 → 蓝牙链路不通，按提示查 hciconfig / rfkill / setcap
#   ✘ 有设备但没锁 → 让人在门口用官方 App 点一次开门的同时再扫

# ④ 拉锁参数、生成配置（自动 chmod 600）
./venv/bin/python yunmei.py 学号 密码 --dump-config yunmei_config.json

# ⑤ 启动并拿到手机地址
./venv/bin/python yunmei_server.py --print-urls
#   打印形如 http://192.168.1.23:8791/?t=xxxx 的地址
```

手机浏览器打开那个地址 → 菜单 →「**添加到主屏幕**」→ 桌面出现"开门"图标。
**点图标 = 开门**（默认打开页面即自动发起开门，见 §4）。

确认没问题后装开机自启：

```bash
sudo cp yunmei.service /etc/systemd/system/
# service 模板里的 __YUNMEI_USER__/__YUNMEI_HOME__ 占位符替换成你的用户名和实际目录：
sudo sed -i "s#__YUNMEI_HOME__#$HOME/yunmei#g; s/__YUNMEI_USER__/$(id -un)/" /etc/systemd/system/yunmei.service
sudo systemctl daemon-reload && sudo systemctl enable --now yunmei
journalctl -u yunmei -f
```

防火墙：`sudo ufw allow from <寝室网段> to any port 8791 proto tcp`

### 唯一会翻车的地方：蓝牙覆盖
`--scan` 报 **rssi 弱于 -75** 或干脆扫不到，说明天线被金属机箱闷住 / 离门太远。
按代价从低到高：
1. USB 延长线把适配器拉到靠门、无遮挡处（**最有效，10 元**）；
2. 换带外置天线的 USB 蓝牙；
3. 门口放 **ESP32-C3（约 15 元）** 做蓝牙中继，服务器 HTTP 驱动它执行 BLE（需要我给固件）；
4. 用 §4 的动态密码兜底（不依赖蓝牙）。

> **combo 网卡的坑（代码已处理）**：蓝牙+WiFi 二合一网卡在"持续扫描"状态下发起 BLE
> 连接经常直接失败。所以服务平时后台监听广播，**开门瞬间主动退出扫描、交还射频**，
> 开完自动恢复。这段有专门测试覆盖（见 §6）。

---

## 3. 秒开是怎么做到的

每次点击都"扫描→连接→写"最长要 10 秒，那不叫"点一下"。三重叠加：

| 手段 | 作用 | 配置 |
|---|---|---|
| 后台常驻监听广播 | 实时知道锁在不在、地址是什么 | `bg_scan: true` |
| 记住上次成功的地址并直连 | 把"现场扫描"变成"直接连接"，1~2 秒 | `learn_mac: true` |
| 保持 BLE 长连接（可选） | 压到亚秒级；个别适配器不稳，默认关 | `keep_connected: false` |

锁会轮换随机蓝牙地址，所以直连失败会**自动回落扫描**并把新地址写回配置，下次又变快。
体感：首次慢（扫描），之后 1~2 秒。

---

## 4. 三条开门路径（互为备份）

| 路径 | 依赖 | 触发 | 何时用 |
|---|---|---|---|
| **A. 桌面图标 → BLE** | 小主机蓝牙能喊到锁 | 点图标 | 日常主路径 |
| **B. 云端动态密码** | 云账号 + 锁带数字键盘 | BLE 失败时页面自动给出；或 `POST /api/dynpwd` | 蓝牙挂了 / 锁不在范围 |
| **C. 官方 App / 实体卡** | — | — | 永远保留的兜底 |

路径 B 是**自动降级**：BLE 失败时服务端顺手调云 API，把动态密码一起回给手机，
页面显示"✘ …｜可在锁键盘输入动态密码 123456"，不至于站在门口抓瞎。

另外 [云莓不智能 APK](https://github.com/zxy19/yunmei_unintelligent) 自带**下拉通知栏磁贴**和
**长按图标的桌面快捷方式**，它本身就是"点一下开门"。暂时不想部署服务器的话，
装 APK + 勾 `quickCon` + 把磁贴拖到常驻区，10 分钟能拿到 90% 的体验——
代价是要求手机自己开蓝牙，且室友不方便共用。

---

## 5. 文件与接口

| 文件 | 说明 |
|---|---|
| `部署操作手册.md` | **照着敲的部署手册**：每步预期输出 + 排障表 |
| `setup.sh` | 一键部署：建目录/venv/依赖/自检；只新增文件，**绝不覆盖已有配置** |
| `yunmei.py` | 云 API 客户端 + 开锁报文构造，**纯标准库、零依赖**，可独立运行 |
| `yunmei_server.py` | 局域网开门服务（需 `bleak`）：HTTP→BLE、PWA 桌面页、动态密码兜底、`--scan` / `--selftest` 诊断 |
| `CONFIG.example.json` | 配置字段逐项说明 |
| `yunmei.service` | systemd 开机自启模板（`setup.sh --service` 自动改路径与用户） |
| `run_tests.py` + `test_*.py` | 统一测试入口，三套共 **71 项断言**，无需蓝牙/真锁/联网 |

**接口**
```
GET  /                     桌面页（一个大按钮；?noauto 关掉"打开即开门"）
GET  /manifest.webmanifest PWA 清单（把 token 固化进 start_url，装完免带参数）
GET  /icon.png             桌面图标（服务端内存生成，无外部素材）
POST /api/open?t=<token>   开门（失败时可能附带动态密码）
POST /api/dynpwd?t=<token> 只取云端动态密码，不占开门冷却
GET  /api/status           健康检查：锁列表、最近结果、广播新鲜度
```

**命令行**
```bash
./venv/bin/python yunmei_server.py --scan              # 部署前闸门：能不能喊到锁
./venv/bin/python yunmei.py 学号 密码 --dump-config yunmei_config.json
./venv/bin/python yunmei.py --secret <lockSecret> --pin 42   # 离线看报文，不联网
./venv/bin/python yunmei.py 学号 密码 --dynpwd <lockNo>      # 单独取动态开门密码
```

---

## 6. 已验证 / 未验证

**本机已验证（63 项断言全绿，不需要蓝牙和真锁）**
- 报文逐字段符合协议；PIN 每次随机；写到 0002、订阅 0003；电量换算与原 App 一致
  （"hex 串按十进制解释"这个坑）。
- 有地址→直连且**不触发扫描**；地址失效→回落扫描并**把新地址写回配置**；扫不到→可行动的错误信息。
- **开门时后台监听交还射频、开完自动恢复**（combo 网卡的关键前提）。
- 非长连接模式下连接必定关闭，不泄漏连接。
- 鉴权（错误/缺失 token 一律 403）、冷却 429、未知路径与未知锁 404、BLE 失败优雅 502。
- PWA：manifest 把 token 固化进 `start_url`；非法 token 不生成带凭据的清单；`&noauto` 生效；
  页面不回显非法 token（防 XSS）；合规 token 原样嵌入、不被 HTML 实体转义破坏。
- 配置护栏：token 含特殊字符/过短 → 拒绝启动并点名；缺配置/坏 JSON/缺 locks →
  人话报错 + 下一步指引，不是一屏 traceback；合规配置能正常起来。
- `--scan` 四条分支：命中报 rssi / 未命中给原因清单 / 0 设备判定"链路不通" /
  适配器报错给 hciconfig·rfkill·setcap 排查项。

**必须你在现场验证（我碰不到你的硬件和账号）**
1. `--scan` 能否命中锁、rssi 多少；
2. 真机 `POST /api/open` 能否真的开门；
3. 云 API 是否还活着、`--dump-config` 能否取到锁的密钥（学校若换了密钥体系，本方案失效）；
4. 动态密码的时效（能否重复使用）。

```bash
py -3 test_smoke.py && py -3 test_ble_logic.py && py -3 test_config_guard.py
```

---

## 7. 安全红线（这类锁的钥匙是静态的，别省）

1. **`lockSecret` 是永久钥匙。** `yunmei_config.json` 必须 `chmod 600`（`--dump-config` 已默认设置），
   不进任何仓库、不发群里。配置里**只存口令 MD5，不存明文口令**。
2. **绝不做公网端口映射。** 只在寝室局域网可达。要"在外面给室友开门"就用 Tailscale（可选，不是前提）。
3. **一人一个 token**，日志能看出谁开的；谁毕业了删掉那一行即可。
   token 只允许 `字母/数字/_/-`、长度 6–80（要嵌进网页脚本，服务会在启动时校验并拒绝不合规的）。
4. **冷却 5 秒**防误触；连续失败先怀疑电量，别猛点。
5. **保留原路径**：官方 App、实体卡／机械钥匙一律不动。
6. **这是学校资产**。自用便利和对外传播接口是两回事；被宿管问就直说。
7. 云端若哪天给报文加了签名/滚动码，A、B 两条会失效——那时只剩 APK 与物理方案。

---

## 8. 还想更省事

| 需求 | 做法 | 成本 |
|---|---|---|
| 走到门口手机碰一下就开 | NFC 标签贴门框（NFC Task Launcher / iOS 快捷指令个人自动化绑这个 URL） | 3 元 |
| 真·无页面闪现 | MacroDroid / Tasker / iOS 快捷指令：桌面图标→后台 POST→Toast 结果 | 0 |
| 室友远程也能开 | 小主机装 Tailscale，URL 换成 `http://<小主机的Tailscale地址>:8791/...` | 0 |
| QQ 里喊一句开门 | AstrBot 加插件，内容就是 `POST http://127.0.0.1:8791/api/open?t=...` + QQ 号白名单 | 0 |
| 蓝牙实在够不着 | 门口 ESP32-C3 中继：`ble_open()` 换成一次 HTTP 调用 | 15 元 |
