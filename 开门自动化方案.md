# 云莓智能宿舍锁 · 免 App 开门自动化方案

> 目标：把"掏手机 → 解锁 → 打开 App → 等加载 → 点开门"压缩成**一步**（碰一下 / 喊一句 / 发一条消息 / 按一下）。
> 现有条件：寝室内一台 Ubuntu 小主机（x86，已部署 AstrBot + NapCat，可选装 Tailscale，地址略）。

---

## 0. 先做这一步：5 分钟判定锁的技术路线

方案选型完全取决于开锁命令走了哪条链路。**先用飞行模式法判定**：

| 测试 | 操作 | 结论 |
|---|---|---|
| T1 | 手机开飞行模式（只单独打开蓝牙）→ 站到门口开门 | **能开** → 路线 A：**BLE 蓝牙直连**（命令在手机侧，不依赖网络） |
| T2 | 飞行模式（蓝牙也关）→ 开门 | **能开** → 路线 C：**NFC 本地**（那就最好办） |
| T3 | 飞行模式下开不了、正常网络能开 | **→ 路线 B：云端下发**（App → 厂商云 → 网关/锁 4G/NB-IoT/LoRa） |
| T4 | 正常网络开门时，用 Reqable / HttpCanary / mitmproxy 抓包，看点击"开门"瞬间有没有一次 HTTPS 请求 | 有 → 确认路线 B，顺手把接口/参数/签名存下来；没有但能开 → 回到路线 A |

补充确认项（看锁的硬件，比看 App 更准）：
- 锁面板上有没有**刷卡区**（写 IC/CARD/NFC）→ 有就是能走实体卡，通常是最省事的解。
- 锁面板上有没有**数字键盘区** → 有密码就有"输密码开门"这条零改造兜底。
- 门内面板有没有**独立的"开门按键/门铃式按钮"**（按住弹锁那种）→ 有就能走物理旁路（方案 F），最稳。
- 门外下方有没有**应急机械钥匙孔** → 有就配一把钥匙，物理世界 10 元解决 80% 的痛点。

---

## 方案矩阵（按性价比排序）

| # | 方案 | 体验档位 | 成本 | 工作量 | 风险/依赖 |
|---|---|---|---|---|---|
| A0 | 问后勤/厂商要**实体门禁卡 / 密码 / 手机 NFC 授权** | ⭐⭐⭐⭐⭐ 碰一下就开 | 0 | 半天沟通 | 需管理方配合 |
| F | **内面板开门键 + ESP32/舵机**物理旁路 | ⭐⭐⭐⭐⭐ 服务器一键，最稳 | 20–50 元 | 1 天 | 属改造设备，须宿管许可 |
| B | **抓 App/小程序接口重放** + AstrBot 插件 | ⭐⭐⭐⭐⭐ QQ 一句话开 | 0 | 0.5–3 天 | 仅适用路线 B；Token 过期、签名/加密 |
| G | **触发层升级**：NFC 标签 / 桌面一键页 / Siri / 地理围栏 | ⭐⭐⭐⭐⭐ | 3–5 元/标签 | 半天 | 依赖 A0/B/F 任一执行通道 |
| E | **闲置安卓机 UI 自动化**（Hamibot/Auto.js） | ⭐⭐⭐⭐ | 0–100 元 | 1–2 天 | App 改版就断，最脏但最通用 |
| D | **BLE 协议复刻**（服务器 USB 蓝牙 / ESP32 中继 + bleak） | ⭐⭐⭐⭐ | 30–80 元 | 2–7 天 | 路线 A 专用；防重放/滚动码可能直接封死 |
| D' | **BLE 报文重放**（抓一次开锁包，原样回灌） | ⭐⭐⭐⭐ | 30 元 | 2 小时 | 廉价锁常可行；带挑战应答/滚动码即失效 |

---

## 一、路线 B（云端下发）：最优雅的解 —— 服务器代开锁

### B1 抓接口
工具优先级：**Reqable / HttpCanary**（Android，最省事）> mitmproxy（PC 代理 + 手机 CA）> Charles。
- 装用户 CA 后能直接看到 HTTPS。若提示证书校验失败 → App 做了 **SSL pinning**，需要 Frida + `objection`/`flutter-frida-ssl-pinning-bypass` 绕，或改用"路由器 tcpdump 只看 host"降级定位。
- 小程序比 App 好抓（微信小程序可开 vconsole / 用 PC 微信 + 代理）。
- 要留存的字段：`host`、`path`、`method`、`body`（doorId / lockId / roomNo / deviceNo）、`Header`（token / sign / timestamp / nonce / 设备指纹）、响应结构、错误码。
- 开门请求连打两次对比，看 `timestamp/nonce/sign` 是否变化 → 判断是否可重放。

### B2 落地架构（不动现有 AstrBot/NapCat 配置，独立服务解耦）

```
QQ 消息 ──NapCat──> AstrBot ──> 插件: open_door ──HTTP──> door-ctl (FastAPI, 127.0.0.1:8790)
                                                                    │
手机 NFC 标签 / 桌面一键页 /ESP32 按钮 ──Tailscale──────────────────┤
                                                                    └──> 厂商云 API 重放（凭据加密存本地）
```

**door-ctl 服务要点**（独立进程 + systemd，便于单独测试、崩了不影响机器人）：
- 只监听 `127.0.0.1` 或 Tailscale 网卡，**绝不公网暴露**。
- 只接受 `POST`，鉴权用 **per-device 长期 token**（不是 GET 上的裸 token，防被爬虫/预览泄露）。
- 白名单：QQ 号 / Tailscale IP 双白名单。
- 限流：同一来源 1 次/5s；连续 3 次失败锁定 5 分钟（防误触发，也防接口被人刷）。
- 审计：`timestamp, source, result, latency` 追加 JSONL 日志；开门失败在群里 @ 全寝。
- Token 刷新：厂商 token 一般有时效，做一次"失效自动用 refresh 凭据重登"的兜底；重登也失效就推消息报警。

### B3 AstrBot 插件（示意，以你机器上 4.26.8 的插件模板为准）

```python
import httpx
from astrbot.api.event import filter, AstrMessageEvent
from astrbot.api.agent import MessageEventResult
from astrbot.api import logger

ALLOWED = {"<室友1的QQ号>", "<室友2的QQ号>"}   # 寝室成员 QQ 号
API = "http://127.0.0.1:8790/open"
TOKEN = "<per-instance token>"

@filter.command("开门")
async def open_door(e: AstrMessageEvent):
    if str(e.get_sender_id()) not in ALLOWED:
        yield MessageEventResult().message("你不是本寝的人，不开。")
        return
    async with httpx.AsyncClient(timeout=8) as c:
        r = await c.post(API, json={"from": f"qq:{e.get_sender_id()}"},
                         headers={"x-auth": TOKEN})
    ok = r.status_code == 200
    yield MessageEventResult().message("已开门 ✔" if ok else f"开门失败 ✘ ({r.status_code})")
```

> 建议：先在服务器上把 door-ctl 用 curl 打通（`curl -X POST -H x-auth:... http://127.0.0.1:8790/open`），确认能开门，再挂机器人插件。顺序反了会分不清是"接口错"还是"插件错"。

---

## 二、路线 A（蓝牙直连）：三条分支，从便宜到贵

1. **D' 报文重放（先试这个，2 小时成本）**
   `nRF Connect for Desktop` + `nRF Sniffer for BLE`（需一块 nRF52840  dongle，30–60 元）抓 `连接 → 服务发现 → 写特征值` 全过程，或用 Android 的蓝牙 HCI snoop log。抓到那条 `Write Characteristic` 后，用 PC/服务器同一 App 流程重放一次：
   - 成功 → 说明无防重放，直接用 `bleak`（Python）在服务器上复刻，配 USB BT4.0 适配器（J1900 是 x86，插一个就行）。
   - 失败（更常见）→ 说明是**挑战-应答 / 滚动码**，重放无效，进入 2 或 3。

2. **D BLE 协议逆向**：反编译 APK（jadx）找 GATT UUID、密钥派生（常见是 AES + 设备密钥 + 时间戳）。可行但工作量以周计，且要拿到 App 里的密钥材料。**不建议**作为首选。

3. **信号中继（实操推荐）**：如果蓝牙覆盖不到门（锁在门外走廊，隔一道防火门是很正常的衰减），不要指望服务器直连。在门口放一个**小蓝牙节点**（ESP32-C3 或树莓派 Zero W，或一台闲置安卓机），服务器经 Wi-Fi 下发 → 门口节点执行 BLE 开锁。

---

## 三、路线 C / 方案 A0（实体凭证）：**别小看这条，它通常是最优解**

- 这类校园锁 90% 带 **IC 卡刷卡区**。管理端后台一般能"发卡/授权/加指纹/加密码"。
- 走宿舍管理员/后勤报修渠道，说明"App 经常打不开、希望加一张卡或一个成员权限"，比逆向整条链路成本低两个数量级，且**完全合规**。
- 拿到卡后：卡本身可以塞进钥匙扣；若是未加密 UID 卡，部分安卓手机/手表的钱包支持模拟（"门卡"功能），**手机熄屏碰门就开**，体验直接满分。
- 有键盘区 → 设一个纯数字密码，配合方案 G 的"密码快捷填充"也能一步开。
- 顺便问一句：厂商有没有**开放 API / 管理员接口 / 临时密码接口**。企业版宿管端通常有，问到就等于路线 B 免逆向。

---

## 四、方案 F：物理旁路（最稳、最可逆、最推荐技术不熟练时选）

前提：门内面板有一个"开门按键"（按住/触摸式电子开门），或锁体带电控开门端子。

- **软触按**：`ESP32-C3` + `SG90 舵机`，机械臂压那个按钮 500ms；或按钮是两针焊盘时用**光耦/继电器**并联（等效按下，不切断原信号，原按钮照常可用）。
- 触发：ESP32 直接 `HTTP GET http://<小主机Tailscale地址>:8790/open-local`（走 Tailscale），或反向由服务器发 MQTT。
- 供电：锁旁边的电池盒**不要取电**（会影响锁的功耗与质保）；ESP32 用独立锂电/干电池 + 深度睡眠，或门内 USB 充电头。
- 优点：与厂商协议完全解耦，App 改版、云端升级、Token 过期统统不影响；失败模式是"按不动"而不是"开了不该开的门"。
- 缺点：属于对宿舍资产加装物件，**必须先取得宿管/后勤同意**，且必须保证**门内机械开启和逃生通道 100% 不受影响**。

```cpp
// ESP32 侧示意：收到信号 → 舵机按下开门键 → 回 0
void loop() {
  if (WiFi.status() == WL_CONNECTED && http.GET() == 200) {
    servo.write(160); delay(500); servo.write(90);   // 按下 → 复位
  }
  delay(200);
}
```

---

## 五、方案 E：UI 自动化（协议搞不动时的万能兜底）

拿一台闲置安卓机（旧手机即可，常年插电、关屏保/关省电限制）放寝室：

- **Hamibot**（自带 HTTP 触发接口，最适合被服务器驱动）/ Auto.js Pro / 按键精灵：无障碍服务自动 `启动 App → 等待按钮 → click() → 截图回传做结果确认`。
- 服务器 → `POST http://手机:8188/run?token=...` → 手机跑脚本 → 把截图/成功标志回给 door-ctl。
- 优点：不碰协议、不涉及逆向，任何锁都能用。
- 缺点：App 一改版就断（要把控件 ID 匹配写得容错 + 失败自动回退到"通知群里手动开"）；手机得在锁的蓝牙范围内（路线 A）或只需有网（路线 B）。

---

## 六、方案 G：触发层（体验真正的差距在这里）

执行通道（B / F / E）任一打通后，加下面任意一个入口。注意"QQ 发消息"其实不比开 App 快多少，真正快的是**碰一碰**和**语音**：

| 入口 | 做法 | 手感 |
|---|---|---|
| **NFC 标签**（3 元/个，贴门边） | 安卓用"NFC Task Launcher"类 App 把标签绑成 `打开 URL(POST 页面)`；iOS 用"快捷指令 → 个人自动化 → NFC → 运行脚本 → 请求 URL" | 掏手机碰门边一下，熄屏可用，**最推荐** |
| **桌面一键页 / PWA** | door-ctl 出一个只含一个按钮的极简页面（加 CSRF 式 token），添加到主屏幕 → 图标即开门 | 比开 App 少 3 步 |
| **Siri / 语音** | 快捷指令里把"开门"URL 存成"嘿 Siri，开门"；或车机/耳机直接触发 | 手里拎东西时救命 |
| **地理围栏 / 连上寝室 Wi-Fi 自动开** | 手机检测到进 5m/连上指定 SSID → 自动请求开门 | 最无感，但**误触发与安全风险最高**，建议只做成"手机振一下，你确认再开" |
| **QQ 群消息**（AstrBot + NapCat） | `/开门` 或群关键词，附带开门人昵称 + 时间写进群日志 | 适合"给回来的室友远程开门"，不适合自己日常 |
| **实体按钮**（门边一个无线开关） | ESP32/433 开关 → 服务器 → 开门；或方案 F 的 ESP32 直接本地执行 | 手机没电时的兜底，实测最可靠 |

---

## 七、安全与合规红线（这几条别省）

1. **合规**：宿舍门锁是学校资产、也是公共门禁。逆向/加装前优先走后勤或宿管的许可与报修渠道；私自改动物件在很多校规里是可以处分的。优先选方案 A0（申请发卡）与 F（报备后加装）。
2. **接口一旦上网，就是"把门锁钥匙放在网络上"**：只走 Tailscale/内网、只允许 POST、per-device token、QQ/IP 双白名单、限流、失败锁定、全量审计。别用 `GET /open?key=xxx`（会进浏览器历史、代理日志、CDN 缓存）。
3. **兜底不砍**：原 App、实体卡/机械钥匙路径必须保留；任何改造都不得影响**门内机械开启与消防逃生**。
4. **断电可用**：door-ctl 挂了、机器人挂了、网络断了，上面的 NFC/物理按钮/F 方案仍能本地开门（把本地通道设计成不依赖服务器也行）。
5. **别影响锁的功耗与质保**：不要从锁的电池取电、不要持续高频唤醒锁；这类锁频繁被云端唤醒会明显掉电。
6. **别做成"自动开门"**：自动感应开门在有人尾随/误触发时是真风险，默认做"一键确认"而不是"无人确认"。

---

## 八、建议的推进顺序

1. **今天（0 成本）**：做 §0 的 T1–T4 判定；同时扒一眼锁面有没有刷卡区/键盘/内开门键；顺手问宿管能不能加一张卡或加个成员权限。
2. **路线 B** → 抓接口 → 服务器 curl 打通 → 写 door-ctl（含鉴权/限流/日志）→ AstrBot 插件 → 贴 NFC 标签。预计 1 天，最优雅。
3. **路线 A** → 先花 2 小时试 D' 报文重放；失败就直接上方案 E（闲置手机 + Hamibot）或 F。
4. **只想赶紧不难受** → 直接方案 F 或 A0，别碰协议。

## 待确认信息（决定选哪条路）
1. 飞行模式测试（T1/T2/T3）的结果 → 确定路线 A/B/C。
2. 锁的品牌型号（锁体铭牌或 App 里"设备信息"）+ 面板有没有刷卡区/键盘/内开门键。
3. 门内到寝室内（放服务器/手机的位置）蓝牙能不能稳定连上锁。
4. 宿管/后勤对"加装一个电子开门按钮"或"再发一张卡"的态度。
